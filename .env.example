# Environment Variables Template
# Copy this file to .env and fill in your actual values

# Production URL (when deploying to your own domain)
VITE_PRODUCTION_URL=https://yourdomain.com

# Play Store URL (add this when you publish to Google Play Store)
VITE_PLAY_STORE_URL=https://play.google.com/store/apps/details?id=com.yourcompany.tictac3x5

# Replit Domain (usually auto-detected, only set if needed)
VITE_REPLIT_DOMAIN=your-repl-name.your-username.repl.co

# Database URL (already configured in Replit)
DATABASE_URL=postgresql://...

# Email configuration (if using custom SMTP)
SENDGRID_API_KEY=your_sendgrid_api_key